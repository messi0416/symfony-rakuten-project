# -----------------------------
# webシステム 受注取込明細設定 から作成されたcrontab ファイルです。
# data/cron/auto 以下に配置され、設定更新時にcrontab へ追記されます。

{% for schedule in schedules %}
{% if schedule.active == -1 %}
# auto generated setting: 受注取込明細設定 [ {{ dayOfWeek[schedule.dayOfWeek] }}]
{{ schedule.minutes }} {{ schedule.hours }} * * {{ schedule.dayOfWeek }} /home/workuser/ne_api/app/console --env={{ env }} {{ schedule.command }}

{% endif %}
{% endfor %}
